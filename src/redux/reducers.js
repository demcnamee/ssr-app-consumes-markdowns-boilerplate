//import { REQUEST_APPS, RECEIVE_APPS } from './actions';

function apps(state = { isFetching: false, apps: [] }, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default apps;
